#pragma once

//All modules headers must be included here
#include "Version.h"

extern String (*fun_ptr)(void); //Function pointer to the function that will be called when the key is pressed (In which module should be activated